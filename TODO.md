# TODO

Актуальный бэклог. Post-MVP фичи (звук, LLM, голос) — в `README.md`.

## Баги

- [x] **Окно не реагирует при первом запуске** / **после автозапуска таймер
  не стартует, в трее вечное `Nudge`.** Один корень: при старте без активации
  окна (автозапуск из `HKCU\...\Run`, фоновый запуск) winit не получает ни
  одного события и eframe вообще не рисует первый кадр — окно висит
  невидимым, `hwnd` не передан трею (Show/хоткей мертвы), а замороженный по
  спеке §4 таймер нечем запустить. Плюс начальный popup миновал `show_popup`
  и не получал фокус. Починено в `src/app.rs`: `request_repaint()` в
  конструкторе форсирует первый кадр; начальный popup идёт через
  `show_popup()` (единый путь показа, `force_foreground`). Проверено живьём
  на Windows через запуск планировщиком (симуляция автозапуска).

## Известные падения e2e (пре-экзистинг, есть и на чистом master)

- [ ] `settings.test.ts` → hotkey recorder captures Ctrl+Shift+A: рекордер
  не ловит комбо, персистится старый `Ctrl+Shift+Space`.
- [ ] `design-static.test.ts` → placeholder hint text is legible: peak
  brightness 58 при пороге 140.

## Окно настроек

Открывается из трея (правый клик → `Settings`) как отдельный процесс
`nudge.exe --settings`. Сейчас содержит три поля:

- [x] **Хоткей.** Кнопка «Запись» — пользователь нажимает желаемую комбо, мы её ловим (через `hotkey::hotkey_from_egui`), канонизируем и подставляем в поле. Save персистит как обычно. Bare Escape отменяет запись.
- [x] **Автозапуск с Windows.** Тумблер. Транзакционно: пишем в `HKCU\Software\Microsoft\Windows\CurrentVersion\Run`, подтверждаем через `is_enabled`, и только тогда фиксируем в config.json. На ошибку — плашка, чекбокс не сдвигается.

- [x] **Live-reload основного процесса** при изменении config.json. Watcher через `notify` следит за `<Documents>/Nudge/config.json` и сигналит тред трея перечитать файл; единственный живой эффект — перерегистрация глобального хоткея, остальные поля обновляются в кэше без UI-действий.

Скрытое из этого батча:

- ~~**Аниме-спрайт над popup.**~~ Снято с дорожной карты этого окна.

## Рассинхрон кода со спекой

- [ ] **Frosted-glass backdrop на Windows.**
  Спека §8 сама помечает как открытое: acrylic/mica за карточкой через eframe. Сейчас только `Color32::TRANSPARENT` для panel/window — настоящего blur за окном нет.

## Технический долг

- [ ] **`WS_EX_TOOLWINDOW` не держится.** Первый кадр ставит стиль через
  `SetWindowLongPtrW` (`src/app.rs`), но живой exstyle окна — `0x00040118`,
  без бита 0x80: winit перезатирает exstyle своим кэшем флагов. Скорее всего
  окно видно в ALT+TAB вопреки замыслу. Наблюдение с живого Windows
  (2026-07-08); чинить, вероятно, через `with_window_level`/винитовские флаги
  вместо ручного Win32.

- [ ] **Optional поля в журнале.**
  `src/journal.rs:16` TODO: `prompt_version`, `input_method`, `metadata`. Сейчас не сериализуются. Контракт в `docs/journal-spec.md`.
