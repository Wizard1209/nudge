# TODO

Актуальный бэклог. Post-MVP фичи (хоткей, звук, автозапуск, LLM, голос) — в `README.md`.

## Рассинхрон кода со спекой

- [x] **Switch-away: клик мимо / потеря фокуса скрывают попап, но сохраняют поля.**
  Спека §4 переписана через таблицу (Enter / Esc / Switch × пишет / резетит таймер / чистит поля). Switch = Esc минус очистка. В коде: новый `Action::SwitchAway` в `nudge_state.rs`, ветка в `hide_popup` (не чистит `doing`/`bullshit`), пути click-outside и focus-loss в `app.rs` маршрутизированы на `SwitchAway`. `card_rect` чистится при любом закрытии (был баг со stale rect при reopen). Спека синхронизирована в `nudge-electron/docs/`. E2e тесты `design-click-outside` и `design-focus-loss` переписаны: после события — карточка скрыта, после reopen через pill введённый текст всё ещё в полях и Enter его пишет в журнал.

- [ ] **Таймер не тикает до первого Enter/Esc.**
  Спека §4: "Таймер стартует только после первого Enter/Esc". Сейчас `Timer::new(10 min)` стартует в `NudgeApp::new` (`src/app.rs:83`). Нужен флаг "ещё не было ни одного закрытия" → таймер заморожен; `reset` после первого `hide_popup` его размораживает.

## Открытые задачи из спеки

- [ ] **Динамическая иконка трея с цифрой минут.**
  Спека §5: скруглённый квадрат, светло-серый фон, цифра оставшихся минут (5×7 bitmap, тёмно-серый текст). Сейчас иконка статичная. Нужен рендер иконки в памяти + обновление раз в минуту вместе с tooltip (`src/tray_bridge.rs`).

- [ ] **Frosted-glass backdrop на Windows.**
  Спека §8: acrylic/mica за карточкой через eframe. Сейчас только полупрозрачный fill карточки поверх transparent-окна — настоящего blur за окном нет.

- [ ] **Wallpaper-картинка за карточкой в WASM.**
  Спека §8: статичная картинка-обои в `e2e/dist/`, рисуется за карточкой — чтобы прозрачность и тень были видны при верификации в браузере.

## Технический долг

- [ ] **Optional поля в журнале.**
  `src/journal.rs:16` TODO: `prompt_version`, `input_method`, `metadata`. Сейчас не сериализуются. Контракт в `docs/journal-spec.md`.
